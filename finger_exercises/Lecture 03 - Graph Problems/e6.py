# In the following examples, assume all graphs are undirected. That is, an edge
# from A to B is the same as an edge from B to A and counts as exactly one
# edge.
#
# A clique is an unweighted graph where each node connects to all other nodes.
# We denote the clique with  nodes as KN. Answer the following questions in
# terms of .
#
#
#
#
#
#
# 1) What is the asymptotic worst-case runtime of a Breadth First Search on KN?
# For simplicity, write  as just n,  as n^2, etc.
#
# ans: n
#
#
#
#
# 2) BFS will always run faster than DFS.
#
# True
#
# Ans: False
#
#
#
# 3) If a BFS and DFS prioritize the same nodes (e.g., both always choose to
# explore the lower numbered node first), BFS will always run at least as fast
# as DFS when run on two nodes in KN.
#
#
# ans: True
#
# False
#
#
#
#
# 4) If a BFS and Shortest Path DFS prioritize the same nodes (e.g., both always
# choose to explore the lower numbered node first), BFS will always run at
# least as fast as Shortest Path DFS when run on two nodes in any connected
# unweighted graph.
#
# ans: True
#
# False
#
#
#
# 5) Regardless of node priority, BFS will always run at least as fast as Shortest
# Path DFS on two nodes in any connected unweighted graph.
#
# ans: True
#
# False
